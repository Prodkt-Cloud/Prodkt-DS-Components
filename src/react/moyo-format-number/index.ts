import * as React from 'react';
import { createComponent } from '@lit-labs/react';
import Component from '../../components/format-number/format-number';

export default createComponent(React, 'moyo-format-number', Component, {});
