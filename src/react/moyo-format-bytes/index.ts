import * as React from 'react';
import { createComponent } from '@lit-labs/react';
import Component from '../../components/format-bytes/format-bytes';

export default createComponent(React, 'moyo-format-bytes', Component, {});
